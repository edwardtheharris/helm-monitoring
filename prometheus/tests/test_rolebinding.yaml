###
# ```{rubric} Role Binding Tests
# ```
# ---
# Test suite for role binding.
#
# ```{literalinclude} prometheus/tests/test_rolebinding.yaml
# :language: yaml
# :caption: tessts/test_rolebinding.yaml
# :start-at: "suite: 'RoleBinding test suite'
# ```
suite: "Role Binding Test Suite"
release:
  name: prometheus
templates:
  - templates/rolebinding.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
