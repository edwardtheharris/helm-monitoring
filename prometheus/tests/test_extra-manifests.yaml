###
# ```{rubric} Extra Manifests Tests
# ```
# ---
# Test suite for cluster roles.
#
# ```{literalinclude} prometheus/tests/test_extra-manifests.yaml
# :language: yaml
# :caption: tessts/test_extra-manifests.yaml
# :start-at: "suite: 'ExtraManifest test suite'
# ```
suite: "Extra Manifests Test Suite"
release:
  name: prometheus
templates:
  - templates/extra-manifests.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
