###
# ```{rubric} ServiceAccount Tests
# ```
# ---
# Test suite for service accounts.
#
# ```{literalinclude} prometheus/tests/test_serviceaccount.yaml
# :language: yaml
# :caption: tessts/test_serviceaccount.yaml
# :start-at: "suite: 'serviceaccount test suite'
# ```
suite: "ServiceAccount Test Suite"
release:
  name: prometheus
templates:
  - templates/serviceaccount.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
