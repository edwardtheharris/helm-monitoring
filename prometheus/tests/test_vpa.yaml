###
# ```{rubric} VPA Tests
# ```
# ---
# Test suite for vpas.
#
# ```{literalinclude} prometheus/tests/test_vpa.yaml
# :language: yaml
# :caption: tessts/test_vpa.yaml
# :start-at: "suite: 'vpa test suite'
# ```
suite: "VPA Test Suite"
release:
  name: prometheus
templates:
  - templates/vpa.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
