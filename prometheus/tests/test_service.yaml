###
# ```{rubric} Service Tests
# ```
# ---
# Test suite for services.
#
# ```{literalinclude} prometheus/tests/test_service.yaml
# :language: yaml
# :caption: tessts/test_service.yaml
# :start-at: "suite: 'service test suite'
# ```
suite: "Service Test Suite"
release:
  name: prometheus
templates:
  - templates/service.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
