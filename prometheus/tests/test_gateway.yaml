###
# ```{rubric} Gateway Tests
# ```
# ---
# Test suite for gateways.
#
# ```{literalinclude} prometheus/tests/test_gateway.yaml
# :language: yaml
# :caption: tessts/test_gateway.yaml
# :start-at: "suite: 'gateway test suite'
# ```
suite: "Gateway Test Suite"
release:
  name: prometheus
templates:
  - templates/gateway.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
    values:
      - ../thw.values.yaml
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
    values:
      - ../comms.values.yaml
