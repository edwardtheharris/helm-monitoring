###
# ```{rubric} Network Policy Tests
# ```
# ---
# Test suite for cluster roles.
#
# ```{literalinclude} prometheus/tests/test_network-policy.yaml
# :language: yaml
# :caption: tessts/test_network-policy.yaml
# :start-at: "suite: 'NetworkPolicy test suite'
# ```
suite: "Network Policy Test Suite"
release:
  name: prometheus
templates:
  - templates/network-policy.yaml
values:
  - ../values.yaml
tests:
  - it: should match the snapshot
    asserts:
      - matchSnapshot: {}
