###
# ```{rubric} pdb Test Suite
# ```
# ---
# Tests to validate the pdb of the described networkpolicy.
#
# ```{literalinclude} /tests/pdb_test.yaml
# :language: yaml
# :start-at: "suite: test for pdb\n"
# :end-before: "###\n"
# ```
suite: test for pdb
release:
  name: kube-state-metrics
templates:
  - templates/pdb.yaml
values:
  - ../values.yaml

###
# ```{rubric} pdb Tests
# ```
# ---
# Tests for pdb Template
#
# ```{literalinclude} /tests/pdb_test.yaml
# :language: yaml
# :start-at: "tests:\n"
# ```
tests:
  - asserts:
      - isKind:
          of: PodDisruptionBudget
      - matchSnapshot: {}
    set:
      podDisruptionBudget:
        enabled: true
