###
# ```{rubric} role Test Suite
# ```
# ---
# Tests to validate the role of the described networkpolicy.
#
# ```{literalinclude} /tests/role_test.yaml
# :language: yaml
# :start-at: "suite: test for role\n"
# :end-before: "###\n"
# ```
suite: test for role
release:
  name: kube-state-metrics
templates:
  - role.yaml
values:
  - ../values.yaml

###
# ```{rubric} role Tests
# ```
# ---
# Tests for role Template
#
# ```{literalinclude} /tests/role_test.yaml
# :language: yaml
# :start-at: "tests:\n"
# ```
tests:
  - asserts:
      - isKind:
          of: Role
      - matchSnapshot: {}
