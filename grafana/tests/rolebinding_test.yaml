###
# ```{rubric} RoleBinding Test Suite
# ```
# ---
# Tests to validate the deployment of the described RoleBinding.
#
# ```{literalinclude} /tests/rolebinding_test.yaml
# :language: yaml
# :start-at: "suite: test for roles\n"
# :end-before: "###\n"
# ```
suite: test for roles
release:
  name: grafana
templates:
  - templates/rolebinding.yaml
values:
  - ../values.yaml

###
# ```{rubric} RoleBinding Tests
# ```
# ---
# Tests for RoleBinding Template
#
# ```{literalinclude} /tests/rolebinding_test.yaml
# :language: yaml
# :start-at: "tests:\n"
# ```
tests:
  - it: should be a RoleBinding
    asserts:
      - isKind:
          of: RoleBinding
  - it: RoleBinding should match snapshot
    asserts:
      - matchSnapshot: {}
