###
# ```{rubric} Clusterclusterrole Test Suite
# ```
# ---
# Tests to validate the deployment of the described Clusterclusterrole.
#
# ```{literalinclude} /tests/clusterrole_test.yaml
# :language: yaml
# :start-at: "suite: test for clusterroles\n"
# :end-before: "###\n"
# ```
suite: test for clusterroles
release:
  name: grafana
templates:
  - templates/clusterrole.yaml
values:
  - ../values.yaml

###
# ```{rubric} Clusterclusterrole Tests
# ```
# ---
# Tests for Clusterclusterrole Template
#
# ```{literalinclude} /tests/clusterrole_test.yaml
# :language: yaml
# :start-at: "tests:\n"
# ```
tests:
  - it: should be a ClusterRole
    asserts:
      - isKind:
          of: ClusterRole
  - it: ClusterRole should match snapshot
    asserts:
      - matchSnapshot: {}
