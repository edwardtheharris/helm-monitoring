---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  ports:
    - name: >-
        {{ .Release.Name }}
      port: 80
      targetPort: 3000
  selector:
    app.kubernetes.io/name: >-
      {{ .Release.Name }}
  type: LoadBalancer
